package utils;
import io.cucumber.datatable.DataTableType;
import models.RegisterModel;

/*public class tableTransform implements DataTableType<RegisterModel>{

     @Override
        public RegisterModel transform(Map<String, String> entry) {
            RegisterModel model = new RegisterModel();
            model.setName(entry.get("name"));
            model.setEmailAddres(entry.get("emailAddres"));
            model.setPassword(entry.get);
            return model;
        }
    }


}*/
